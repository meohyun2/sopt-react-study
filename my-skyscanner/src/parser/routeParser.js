
const routeParser = (body) => {
  let result = {
    
  }
  if(body.Quotes&&body.Places&&body.Carriers){
    // 데이터 전부 들어있다.
    let tempDate = {
      
    }
  }else{
    // error 반환
    return null;
  }
};

module.exports = quoteParser;